-- SELECT *FROM HR.EMPLOYEES
-- SELECT EMPLOYEE_ID,SALARY,RANK() OVER(ORDER BY SALARY DESC) AS RANK FROM HR.EMPLOYEES
-- SELECT EMPLOYEE_ID, SALARY, DENSE_RANK()OVER(ORDER BY SALARY DESC) AS DENSERANK FROM HR.EMPLOYEES
-- SELECT EMPLOYEE_ID,SALARY,ROW_NUMBER() OVER(ORDER BY SALARY DESC) AS ROWNUMBER FROM HR.EMPLOYEES
-- SELECT EMPLOYEE_ID,DEPARTMENT_ID,SALARY,RANK()OVER(PARTITION BY DEPARTMENT_ID ORDER BY SALARY DESC) AS DEPARTMENT_WISE_RANK FROM HR.EMPLOYEES
--  SELECT EMPLOYEE_ID,DEPARTMENT_ID,SALARY,DENSE_RANK()OVER(PARTITION BY DEPARTMENT_ID ORDER BY SALARY DESC) AS DEPARTMENT_WISE_DENSE_RANK FROM HR.EMPLOYEES
--  SELECT EMPLOYEE_ID,DEPARTMENT_ID,SALARY,ROW_NUMBER()OVER(PARTITION BY DEPARTMENT_ID ORDER BY SALARY DESC) AS DEPARTMENT_WISE_ROW_NUMBER FROM HR.EMPLOYEES
